# Enrollment Flow

Each family member records several voice samples through the kiosk UI, which are sent to the audio service to generate Resemblyzer speaker embeddings stored for real-time identification.
